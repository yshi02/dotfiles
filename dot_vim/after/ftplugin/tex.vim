" set tab width to 2 spaces
setlocal tabstop=2

" set indent shift width to 2 spaces
setlocal shiftwidth=2

" replace tabs with spaces
setlocal expandtab

" enable folding based on syntax (sections, environments)
setlocal foldmethod=syntax
